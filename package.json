{
  "name": "dx",
  "private": true,
  "description": "My shared configs and developer experience helpers for my projects",
  "repository": {
    "url": "https://github.com/will-stone/dx"
  },
  "license": "MIT",
  "author": "Will Stone",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint .",
    "release:eslint-config": "yarn workspace @will-stone/eslint-config publish",
    "release:prettier-config": "yarn workspace @will-stone/prettier-config publish"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{css,json,md}": [
      "prettier --write"
    ],
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix"
    ]
  },
  "prettier": "./packages/prettier-config/index.json",
  "eslintConfig": {
    "extends": [
      "./packages/eslint-config/index.js",
      "./packages/eslint-config/node.js"
    ]
  },
  "devDependencies": {
    "eslint": "^7.3.1",
    "husky": "^4.2.5",
    "lint-staged": "^10.2.9",
    "prettier": "^2.0.5"
  }
}
